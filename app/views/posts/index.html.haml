.posts-wrapper.row
  -@posts.each do |p|
    .post
      .post-head
        .name
          Jasenko
      .image-center-block
        =link_to (image_tag p.image.url(:medium), class:'img-responsive'), post_path(p)
      %p.caption
        = p.caption
